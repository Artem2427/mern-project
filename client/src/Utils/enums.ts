export enum Methods {
  get = "GET",
  post = "POST",
  patch = "PATCH",
  delete = "DELETE",
}

export enum TypeNotification {
  success = "success",
  erorr = "error",
}

export enum NavTabs {
  links = "links",
  createLink = "create-link",
  home = "home",
  login = "login",
}
